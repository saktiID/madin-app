<div class="text-center">
    <label class="switch s-primary mr-2">
        <input type="checkbox" {{ ($is_active) ? 'checked' : '' }} class="activate" data-id="{{ $id }}" data-nama="{{ $nama }}">
        <span class="slider round"></span>
    </label>
</div>
